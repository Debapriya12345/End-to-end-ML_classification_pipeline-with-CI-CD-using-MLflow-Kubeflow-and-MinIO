### 📌 I have created 2 models(Random Forest and Gradient Boosting and stored in ML flow registry)

### 📌 Also created FastApi UI which hits the localhost url and giving me predection result(0/1)